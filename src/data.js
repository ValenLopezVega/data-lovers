
export const filtrado = (arrData) => {
 /* const arrFit = arrData.sort((a,b) => {
    if (a.name < b.name) {
      return -1;  
    }
    if (a.name > b.name) {
      return 1;
    }
    return 0;
  })*/
 console.log( arrData);
};

export const anotherExample = () => {
  return 'OMG';
}
